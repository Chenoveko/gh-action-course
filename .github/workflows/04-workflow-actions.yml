name: 04 - Workflow Actions
on: push

jobs:
  build-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
      - name: Printing Folder
        run: ls